# node
node_modules
.next
out

# python
.venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache

# misc
.DS_Store
# .cursor/rules/ is intentionally tracked — project coding standards for Cursor IDE
.cursor/settings.json

# secrets — never commit real env files
.env
.env.local
.env.*.local
web/.env.local
ingest/.env

# .env.example is intentionally committed as a blank template

# eval results — generated artefacts, never commit
ingest/eval/results/